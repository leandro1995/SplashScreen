<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SplashScreenExample" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/teal_700</item>
    </style>

    <!-- Splash Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/teal_700</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_git_hub</item>
        <item name="postSplashScreenTheme">@style/Theme.SplashScreenExample</item>
    </style>

</resources>